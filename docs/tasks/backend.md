# 백엔드 작업 목록

- 상태: M2 구현 및 외부 API·장소 추천 실호출 검증 완료, 컨테이너·Cloud Run·성능 검증 대기
- 갱신 기준일: 2026-08-20
- 기준 문서: [PRD](../1-prd.md)
- 전체 현황: [통합 작업 현황](../2-task_list.md)
- 범위: 외부 API 검증, FastAPI 분석 서버, 테스트, Docker 및 Cloud Run 배포

> 작업 번호와 완료 조건은 최초 작업 목록을 그대로 유지한다. 실제 인증키와 사용자 입력은 문서·테스트 Fixture·로그에 기록하지 않는다.

## 1. M0 — 외부 API 및 제품 전제 검증

### T-001 외부 API 계정과 키 준비

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: 없음
- 작업:
  - 공공데이터포털에서 한국관광공사 국문 관광정보 API 활용 신청
  - 방문객 API 활용 신청 및 사용 가능 기능 확인
  - 기상청 단기예보 API 활용 신청
  - NAVER Maps Application 생성
  - Android 패키지 이름과 iOS Bundle ID 확정
  - NAVER 모바일 지도 SDK와 REST API 사용 항목 활성화
- 완료 조건:
  - 개발 환경에서 각 API의 최소 요청이 성공한다.
  - API별 호출 한도와 운영계정 전환 절차가 기록되어 있다.

> 한국관광공사 행사·방문객, 기상청 단기예보, NAVER Maps와 NAVER API HUB Local Search 인증정보를 준비했다. Maps와 Local Search 인증정보는 별도 백엔드 환경변수로 관리한다.

### T-002 NAVER 지도·경로 응답 검증

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-001
- 작업:
  - 주소 검색 또는 Geocoding 응답 필드 확인
  - Directions 응답에서 거리, 예상시간 및 경로 좌표 추출 가능 여부 확인
  - 서울역→부산 해운대와 같은 장거리 경로 응답 크기 측정
  - 모바일 지도 SDK에서 경로 좌표를 Polyline으로 표시하는 최소 예제 확인
- 완료 조건:
  - 백엔드에서 사용할 내부 경로 모델과 NAVER 응답 필드 매핑이 문서화되어 있다.
  - Android와 iOS에서 지도 표시 방식이 확정되어 있다.

> 실제 Geocoding·Directions 응답과 서울역→부산역 장거리 경로를 확인했고 iOS Simulator에서 네이티브 지도·경로선을 검증했다. Android 에뮬레이터·실기기와 iOS 실기기 검증은 남아 있다.

### T-003 관광공사 행사 API 검증

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-001
- 작업:
  - `locationBasedList2`의 `contentTypeId=15` 조회 확인
  - `searchFestival2` 날짜 검색 동작 확인(현재 MVP 분석에서는 후속 범위)
  - `detailCommon2`와 `detailIntro2` 응답 필드 확인
  - `MobileOS=IOS`, `MobileOS=AND` 요청 확인
  - Encoding 키와 Decoding 키 사용 방식 확인
  - 정상, 결과 없음, 한도 초과 및 잘못된 키 오류 응답 수집
- 완료 조건:
  - 행사 표준 모델에 필요한 필드가 모두 매핑되어 있다.
  - 테스트 Fixture로 사용할 비밀정보 제거 응답 샘플이 준비되어 있다.

> 실제 행사 목록 응답을 확인했고, 현재 MVP에서는 목적지 반경 15km `locationBasedList2` 1회 호출과 행사 개수 집계만 수행한다. 행사 상세 조회는 후속 단계로 보류한다.

### T-004 방문객·날씨 API 검증

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-001
- 작업:
  - 시도·시군구 방문객 API의 지원 기간과 지역 코드 확인
  - 미래 날짜 요청 시 사용할 과거 참고일 선택 규칙 결정
  - 관광지 집중률 응답의 상대값 의미 확인
  - 기상청 격자 변환과 발표 시각 규칙 확인
  - 예보 제공 전, 결과 없음 및 장애 응답 확인
- 완료 조건:
  - 방문객 참고일 결정 규칙이 테스트 가능한 형태로 정의되어 있다.
  - 날씨 상태가 `available`, `not-yet-published`, `failed`로 구분된다.

> 방문객·집중률과 기상청 단기예보 실응답을 확인했으며, iOS Simulator 분석 결과에서 날씨 상태와 미발표 안내를 확인했다.

### T-005 MVP 외부 공식 페이지 범위 확정

- 상태: `[x]`
- 우선순위: `P1`
- 의존성: T-003
- 작업:
  - 관광공사 응답의 공식 홈페이지 URL만 표시할지 결정
  - 요청 중 공식 페이지 내용을 확인할 출처가 있다면 허용 목록 정의
  - 허용 목록이 없으면 MVP에서 원문 수집 기능을 비활성화
- 완료 조건:
  - 범위가 코드 설정과 PRD에 일치한다.
  - 임의 URL 프록시 또는 무제한 크롤링 경로가 없다.

> MVP는 제공자가 반환한 공식 URL 표시만 허용하고, 임의 URL 프록시·대규모 크롤링은 구현하지 않는 것으로 확정했다.

## 2. M1 — 백엔드 기반 구성

### T-012 FastAPI 프로젝트 초기화

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-010
- 작업:
  - Python 패키지와 `pyproject.toml` 생성
  - FastAPI 애플리케이션 팩토리 또는 진입점 구성
  - Uvicorn 실행 설정
  - Ruff, mypy 및 pytest 설정
  - `/health` 엔드포인트 구현
- 완료 조건:
  - 로컬에서 FastAPI가 실행된다.
  - 린트, 타입 검사 및 최소 테스트가 통과한다.

### T-013 백엔드 환경설정 모델 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-012
- 작업:
  - Pydantic Settings 기반 설정 모델 구현
  - 필수 API 키 누락 시 시작 실패 처리
  - 요청 제한시간, 목적지 행사 수, 동시 호출 수 설정 추가
  - 개발용 `.env.example`에 키 이름만 제공
- 완료 조건:
  - 실제 키가 저장소에 존재하지 않는다.
  - 설정 누락과 잘못된 숫자 범위 테스트가 통과한다.

### T-015 테스트 Fixture 정책 구성

- 상태: `[x]`
- 우선순위: `P1`
- 의존성: T-003, T-004, T-012
- 작업:
  - 외부 API별 정상·빈 결과·오류 Fixture 정리
  - API 키와 사용자 입력 제거 확인
  - 외부 API를 호출하지 않는 단위 테스트 기본 구성
- 완료 조건:
  - 테스트가 네트워크 없이 반복 실행된다.

## 3. M2 — FastAPI 분석 백엔드

### T-020 입력 검증과 오류 응답 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-013, T-014
- 작업:
  - 여행일 형식과 허용 범위 검증
  - 주소 문자열 최대 길이 검증
  - `clientPlatform` 허용값 검증
  - 반경과 키워드 개수 상한 적용
  - 일관된 오류 코드·메시지 모델 구현
- 완료 조건:
  - 잘못된 입력이 외부 API 호출 전에 4xx로 거부된다.
  - 오류 응답에 내부 예외와 API 키가 포함되지 않는다.

### T-021 비식별 요청 로깅 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-012
- 작업:
  - 무작위 Request ID 미들웨어 구현
  - 엔드포인트, 상태, 지연시간 및 제공자 상태만 구조화 로깅
  - 요청 본문, 좌표, 경로, 외부 URL 및 응답 본문 로깅 금지
  - 예외 로그 Sanitizer 구현
- 완료 조건:
  - 자동 테스트에서 금지 필드와 테스트 API 키가 로그에 나타나지 않는다.

### T-022 공통 HTTP 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-013
- 작업:
  - `httpx.AsyncClient` 수명주기 관리
  - 연결·읽기·전체 제한시간 설정
  - 제한적인 재시도 정책 구현
  - 동시 외부 호출 Semaphore 적용
  - 제공자별 오류를 내부 오류 모델로 변환
- 완료 조건:
  - 타임아웃, 4xx, 5xx 및 잘못된 JSON 테스트가 통과한다.

### T-023 NAVER Geocoding 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-002, T-022
- 작업:
  - Client ID·Secret 헤더 적용
  - 주소 검색과 좌표 반환 구현
  - 결과 없음과 복수 후보 처리 규칙 구현
- 완료 조건:
  - 주소가 내부 좌표 모델로 정상 변환된다.
  - 인증 헤더가 로그와 오류 응답에 노출되지 않는다.

> 주소 Geocoding은 Maps 인증정보를 사용하고, 결과가 없을 때 T-029 Local Search fallback으로 장소명을 보완한다. 두 인증정보는 별도 환경변수로 주입한다.

### T-024 NAVER Directions 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-002, T-022
- 작업:
  - 출발지·목적지 자동차 경로 요청
  - 거리, 예상시간 및 경로 좌표 정규화
  - 경로 없음과 제공자 오류 구분
- 완료 조건:
  - 장거리 경로 Fixture가 내부 경로 모델로 변환된다.
  - 지도에 전달 가능한 좌표 순서가 검증된다.

### T-025 관광공사 행사 목록 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-003, T-022
- 작업:
  - 공공데이터 `serviceKey` 요청 처리
  - `locationBasedList2` 구현
  - `searchFestival2` 구현(현재 MVP 분석 경로에서는 호출하지 않음)
  - `MobileOS`와 `MobileApp` 전달
  - 공공데이터 오류 본문 파싱
- 완료 조건:
  - iOS와 Android 요청에서 올바른 `MobileOS`가 전달된다.
  - 정상·빈 결과·한도 초과 응답이 구분된다.

### T-026 관광공사 행사 상세 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-025
- 작업:
  - `detailCommon2`와 `detailIntro2` 구현
  - 분석당 상세 조회 최대 개수 적용
  - 동일 `contentId`의 요청 내 중복 상세 호출 방지
- 완료 조건:
  - 호출 상한을 초과하지 않는다.
  - 상세 실패 행사는 가능한 목록 정보로 유지하거나 경고와 함께 제외된다.

### T-027 방문객 API 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-004, T-022
- 작업:
  - 시도·시군구 방문객 조회 구현
  - 여행지 행정구역 코드 매핑
  - 참고일과 데이터 성격을 응답에 명시
  - 선택적으로 관광지 집중률 조회 구현
- 완료 조건:
  - 방문객 값이 미래 예측처럼 표현되지 않는다.
  - 데이터 없음과 조회 실패가 구분된다.

> 현재 구현은 과거 같은 날짜 참고일을 사용하고 `signguNm`으로 여행지 지역을 선택한 뒤, 응답의 `signguCode`에서 집중률 API용 지역 코드를 매핑한다. 집중률 조회 실패는 방문객 참고값 전체 실패로 확대하지 않는다.

### T-028 기상청 API 클라이언트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-004, T-022
- 작업:
  - 위경도→기상청 격자 변환
  - 여행일에 적절한 발표 시각 선택
  - 단기예보 조회와 시간대 요약
  - 예보 미발표·결과 없음·조회 실패 구분
- 완료 조건:
  - 대표 좌표의 격자 변환 테스트가 통과한다.
  - 예보 범위 밖 날짜가 `not-yet-published`로 반환된다.

### T-029 NAVER Local Search 장소명·주소 추천 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-001, T-023
- 작업:
  - NAVER API HUB Local Search 호출과 전용 인증변수 지원
  - 장소명·지번 주소·도로명 주소·좌표 후보 정규화
  - `mapx`·`mapy` 10⁷배 정수 좌표 변환
  - `GET /api/v1/locations/suggestions` 엔드포인트 구현
  - Geocoding 결과가 없는 장소명의 fallback 처리
- 완료 조건:
  - `서울역`, `부산역` 후보가 실제 API와 백엔드 경유로 반환된다.
  - 모바일에서 후보를 선택하면 도로명 주소가 입력값으로 반영된다.
  - 전용 Client Secret이 모바일·로그·응답에 노출되지 않는다.

> 실제 API와 iOS Simulator XCUITest에서 후보 표시·주소 선택을 확인했다. 여러 후보가 있는 장소명을 선택 없이 제출할 때는 임의 위치를 피하기 위해 `LOCATION_NOT_FOUND`를 반환한다.

### T-030 경로 단순화와 검색 지점 생성

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-024
- 작업:
  - 경로 좌표 단순화 구현
  - 10~20km 간격 검색 지점 생성
  - 목적지 주변 별도 검색 지점 추가
  - 최대 검색 지점 수 강제
- 완료 조건:
  - 짧은·장거리·중복 좌표 경로 테스트가 통과한다.
  - 어떤 입력에서도 설정된 최대 지점 수를 넘지 않는다.

> 이 기하 유틸리티는 경로 표시·거리 계산과 후속 확장을 위해 유지한다. 현재 MVP 행사 검색은 경로를 샘플링하지 않고 목적지 반경 15km만 조회한다.

### T-031 좌표계 변환과 경로 거리 계산

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-014
- 작업:
  - Shapely와 PyProj를 이용한 Point·LineString 구성
  - 거리 계산에 적절한 투영 또는 지리 계산 방식 적용
  - 행사 지점과 경로 사이 최단 거리 계산
  - 여행지와 행사 사이 거리 계산
- 완료 조건:
  - 알려진 좌표 쌍의 오차 허용 범위 테스트가 통과한다.
  - 경도·위도 순서 뒤바뀜을 검출하는 테스트가 있다.

### T-032 행사 정규화와 날짜 검증

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-025, T-026
- 작업:
  - 관광공사 응답을 런타임 행사 모델로 변환
  - 날짜 문자열 파싱과 포함 조건 검증
  - 종료일 누락 처리
  - 좌표·출처·공식 URL 신뢰도 계산
- 완료 조건:
  - 여행일이 행사 기간 밖인 행사가 제외된다.
  - 좌표 없는 행사는 거리 기반 확정 결과가 되지 않는다.

### T-033 요청 내 행사 중복 제거

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-032
- 작업:
  - 콘텐츠 ID, URL, 제목·날짜·장소 기반 중복 제거
  - 제목 정규화 구현
  - 좌표 거리 기반 보조 판정
- 완료 조건:
  - 여러 검색 지점에서 반환된 동일 행사가 한 번만 표시된다.
  - 유사하지만 서로 다른 행사를 과도하게 합치지 않는 테스트가 있다.

### T-034 혼잡 가능성 판정 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-031, T-032, T-033
- 작업:
  - 높음·보통·낮음·확인 필요 판정 함수 구현
  - 판정 근거 문구 생성
  - 실제 지연시간이 아님을 응답에 명시
- 완료 조건:
  - PRD의 모든 기본 등급 사례가 단위 테스트로 고정되어 있다.

### T-040 분석 오케스트레이터 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-023~T-034
- 작업:
  - 지오코딩→경로→목적지 행사→거리 계산 흐름 연결
  - 경로 확정 후 행사·날씨·방문객 호출 병렬화
  - 제공자별 부분 실패와 경고 조합
  - 전체 요청 제한시간 준수
  - 요청 종료 후 전역 상태에 입력·결과가 남지 않도록 구성
- 완료 조건:
  - 정상 Fixture로 전체 분석 응답이 생성된다.
  - 날씨 또는 방문객 실패 시 행사 결과가 정상 반환된다.
  - 경로 실패 시 명확한 실패 응답이 반환된다.

### T-041 분석 API 엔드포인트 구현

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-020, T-040
- 작업:
  - `POST /api/v1/travel-plan/analyze` 구현
  - 응답 헤더와 상태 코드 정의
  - FastAPI OpenAPI 예제 등록
  - 요청 본문 크기 제한 적용
- 완료 조건:
  - PRD 예제 요청이 스키마에 맞는 응답을 반환한다.
  - 분석 ID 저장 또는 재조회 엔드포인트가 생성되지 않는다.

### T-050 백엔드 단위 테스트 완성

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-020~T-041
- 작업:
  - 클라이언트 매핑 테스트
  - 날짜·중복·거리·혼잡 판정 테스트
  - 입력 상한과 로그 Sanitizer 테스트
  - 부분 실패 테스트
- 완료 조건:
  - 핵심 도메인 분기와 완료 조건이 자동 테스트로 검증된다.

### T-051 백엔드 통합 테스트 작성

- 상태: `[x]`
- 우선순위: `P1`
- 의존성: T-041, T-050
- 작업:
  - MockTransport 또는 HTTP Mock 서버로 전체 분석 흐름 검증
  - 외부 지연, 잘못된 JSON 및 5xx 조합 검증
  - 로그에 사용자 입력과 키가 없는지 검증
- 완료 조건:
  - 실제 외부 API 없이 전체 엔드포인트 테스트가 통과한다.

### T-052 Docker 이미지 구성

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-012, T-041
- 작업:
  - 최소 Python 런타임 Dockerfile 작성
  - 비루트 사용자 실행 검토
  - Cloud Run의 `$PORT` 사용
  - `.dockerignore`에 키·개발 파일 추가
  - `/health` 기반 컨테이너 실행 검증
- 완료 조건:
  - 로컬 컨테이너에서 API와 테스트 요청이 성공한다.
  - 이미지 레이어에 `.env`와 API 키가 없다.

### T-055 백엔드 성능 상한 검증

- 상태: `[-]`
- 우선순위: `P1`
- 의존성: T-040, T-052
- 작업:
  - 목적지 행사 검색·행사 개수 집계·외부 호출 수 측정
  - 메모리와 CPU 사용량 측정
  - 동시 요청 10~20개에서 외부 호출 제한 확인
  - 60초 제한시간 내 완료 가능성 확인
- 완료 조건:
  - 최악 입력에서도 각 상한이 지켜진다.
  - 초기 Cloud Run CPU·메모리·동시성 값이 결정된다.

> 목적지 행사 검색·행사 개수 집계·상세 조회 보류·재시도·타임아웃 상한의 자동 회귀 테스트와 호출 예산 문서화는 완료했다. 컨테이너 및 Cloud Run 동시 요청·메모리 측정은 배포 후보에서 남아 있다.

## 4. M5 — Cloud Run 배포

### T-110 Cloud Run 서비스 생성

- 상태: `[ ]`
- 우선순위: `P0`
- 의존성: T-052
- 작업:
  - 서비스명과 `asia-northeast3` 리전 설정
  - 최소 인스턴스 0 설정
  - 최대 인스턴스 1~2 설정
  - CPU, 메모리, 동시성 및 요청 제한시간 설정
  - 공개 호출 정책 결정
- 완료 조건:
  - `/health`와 분석 API가 배포 환경에서 응답한다.

### T-111 Cloud Run 환경변수 등록

- 상태: `[ ]`
- 우선순위: `P0`
- 의존성: T-001, T-110
- 작업:
  - Cloud Run 콘솔에서 공공데이터 키 등록
  - NAVER Maps Client ID·Secret 등록
  - NAVER API HUB Local Search Client ID·Secret 등록
  - 일반 운영 설정 등록
  - Cloud Run IAM 접근 권한 최소화
  - 키가 배포 명령 기록과 저장소에 없는지 확인
- 완료 조건:
  - 배포 환경 API가 정상 동작한다.
  - 앱에서 서버용 키를 확인할 수 없다.

> 로컬 `.env`에는 Maps와 Local Search 키를 분리해 반영했지만 Cloud Run Revision 등록은 아직 시작하지 않았다.

### T-112 Cloud Run 운영 한도 검증

- 상태: `[ ]`
- 우선순위: `P0`
- 의존성: T-110, T-111
- 작업:
  - 최소 인스턴스 0 확인
  - 최대 인스턴스 상한 확인
  - 콜드 스타트 시간 측정
  - 외부 API 한도 초과 시 동작 확인
- 완료 조건:
  - 비용과 호출량 상한이 의도한 설정으로 적용되어 있다.

### T-113 운영 로그 검증

- 상태: `[ ]`
- 우선순위: `P0`
- 의존성: T-021, T-110
- 작업:
  - Cloud Run 기본 요청 로그 확인
  - 애플리케이션 구조화 로그 확인
  - 사용자 입력과 외부 API URL이 기록되지 않는지 확인
  - 장애 분석에 필요한 최소 정보가 있는지 확인
- 완료 조건:
  - 비저장·비식별 로그 정책이 운영 환경에서도 지켜진다.
