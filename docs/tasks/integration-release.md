# 통합·릴리스 작업 목록

- 상태: M4 자동 검증 및 iOS Simulator 실연동 검증 완료, Android·실기기·Cloud Run·법무 검증 대기
- 갱신 기준일: 2026-08-20
- 기준 문서: [PRD](../1-prd.md)
- 전체 현황: [통합 작업 현황](../2-task_list.md)
- 범위: 저장소 공통 구성, API 계약, CI, 통합·보안·개인정보 검증 및 MVP 완료 판정

> 백엔드와 모바일 양쪽에 영향을 주는 계약이나 완료 조건은 이 문서에서 관리하고, 구현 세부사항은 각 작업 문서를 참조한다.

M4 자동 검증 산출물은 `scripts/check_m4.py`, 백엔드의 `test_m4_limits.py`·`test_m4_security.py`, 모바일의 결과 표현 테스트, [성능·호출 예산 기준](../performance.md), [데이터 출처·이용 고지](../source-attribution.md)다. 실제 Android·iOS, Docker/Cloud Run, 제공자 약관 확인은 외부 환경에서 마무리한다.

## 1. M1 — 공통 프로젝트 기반

### T-010 저장소 디렉터리 구성

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: 없음
- 작업:
  - `/mobile`, `/backend`, `/docs` 디렉터리 구성
  - 루트 README에 개발·테스트·실행 명령 추가
  - Node.js, Python 버전 고정 파일 추가
  - 공통 `.gitignore` 작성
- 완료 조건:
  - 신규 개발자가 README만으로 두 프로젝트를 실행할 수 있다.

### T-014 공통 API 계약 정의

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-011, T-012
- 작업:
  - 여행 분석 요청·응답 스키마 정의
  - 장소명 주소 후보 요청·응답 스키마 정의
  - 행사, 경로, 방문객, 날씨 및 경고 모델 정의
  - `clientPlatform`과 혼잡 등급 Enum 정의
  - FastAPI OpenAPI 문서를 기준으로 모바일 TypeScript 타입 생성 또는 수동 동기화 방식 결정
- 완료 조건:
  - 모바일과 백엔드가 동일한 필드명과 Enum을 사용한다.
  - 예제 요청·응답이 스키마 검증을 통과한다.
- 구현 기준:
  - M1에서는 `backend/app/schemas/travel_plan.py`와 `mobile/src/api/contracts.ts`를 수동 동기화한다.
  - 상세 계약과 부분 성공 규칙은 `docs/3-erd.md`, 예제와 테스트 위치는 `docs/api.md`에 기록한다.

### T-016 CI 기본 검사 구성

- 상태: `[x]`
- 우선순위: `P1`
- 의존성: T-011, T-012
- 작업:
  - 모바일 TypeScript 검사, 린트 및 테스트 명령 구성
  - 백엔드 Ruff, mypy 및 pytest 명령 구성
  - PR 또는 main 브랜치 기준 자동 검사 구성
- 완료 조건:
  - 두 프로젝트의 기본 검사가 자동 실행된다.

### T-017 문서 구조 정리

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: 없음
- 작업:
  - PRD를 `/docs/1-prd.md`로 이동
  - 구현 작업 목록을 `/docs/2-task_list.md`로 생성
  - 문서 내부 상대 링크 수정
- 완료 조건:
  - 핵심 프로젝트 문서가 `/docs` 아래에서 정상적으로 연결된다.

## 2. M4 — 통합·품질 검증

### T-090 로컬 전체 통합 테스트

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-041, T-063~T-070
- 작업:
  - 로컬 FastAPI와 Expo 앱 연결
  - 대표 단거리·장거리 여행 분석
  - iOS와 Android의 `clientPlatform` 전달 확인
  - 부분 실패 시나리오 확인
- 완료 조건:
  - PRD 사용자 시나리오가 양 플랫폼에서 완료된다.

> MockTransport 기반 FastAPI 전체 분석 흐름과 모바일 계약·오류 분류를 자동 검증했다. 실제 Expo 앱을 로컬 서버에 연결해 iOS Simulator에서 장소 추천, 주소 선택, 장거리 분석 및 날씨 상태 표시를 확인했다. Android·실기기와 Cloud Run 후보 Revision 검증은 T-102에서 진행한다.

### T-091 API 키 노출 검사

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-052, T-063
- 작업:
  - Git 파일 전체 Secret 스캔
  - Docker 이미지 레이어 검사
  - Expo 번들과 빌드 결과 검사
  - FastAPI 응답과 오류 응답 검사
  - Cloud Run 로그 검사
- 완료 조건:
  - 서버용 API 키가 어떤 클라이언트·로그·아티팩트에도 나타나지 않는다.

> `scripts/check_m4.py`가 모바일 소스·Expo 산출물·백엔드 로그 경계를 자동 검사하고, FastAPI 오류·로그 검사는 테스트로 확인한다. NAVER Maps와 NAVER API HUB Local Search 전용 비밀변수도 서버 전용 경계에 반영했다. 실제 Docker 이미지 레이어와 Cloud Run 로그 검사는 배포 후보에서 남아 있다.

### T-092 사용자 입력 비저장 검증

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-021, T-062, T-090
- 작업:
  - 앱 재시작 후 입력·결과 소멸 확인
  - AsyncStorage와 SecureStore 사용 여부 검사
  - 서버 파일시스템 쓰기와 전역 결과 캐시 검사
  - 로그에서 출발지·목적지·좌표 검색
- 완료 조건:
  - PRD에 정의한 사용자 정보가 영구 저장되지 않는다.

> 모바일은 메모리 전용 Context 상태이며 reset 테스트를 통과한다. 모바일 영구 저장소 marker, 백엔드 저장소 marker, 로그의 입력 필드는 자동 검사한다.

### T-093 외부 API 호출 상한 검증

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-055, T-090
- 작업:
  - 최대 거리·반경·키워드 입력으로 호출 수 측정
  - 목적지 행사 검색, 개수 집계 및 동시 호출 상한 확인
  - 제공자 장애 시 재시도 폭증 여부 확인
- 완료 조건:
  - 요청당 최악 호출량이 문서화되고 설정값을 초과하지 않는다.

> 목적지 행사 검색·행사 개수 집계·상세 조회 보류·동시성·재시도 상한을 Mock provider와 HTTP client 테스트로 확인했으며, 계산 기준은 [성능·호출 예산 기준](../performance.md)에 기록했다.

### T-094 성능·타임아웃 테스트

- 상태: `[-]`
- 우선순위: `P1`
- 의존성: T-090
- 작업:
  - 정상 요청의 p50·p95 측정
  - 동시 요청에서 지연과 메모리 측정
  - 60초 서버 제한과 모바일 제한시간 정합성 확인
- 완료 조건:
  - 목표 응답시간과 허용 실패 조건이 릴리스 문서에 기록되어 있다.

> 서버 60초와 모바일 55초 제한, timeout 오류 변환 및 목표 p50/p95를 문서화했다. 실제 Cloud Run 동시 요청·메모리·p50/p95 측정은 후보 Revision에서 남아 있다.

### T-095 오류 문구와 데이터 표현 검수

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-070, T-090
- 작업:
  - 혼잡 가능성과 실제 지연시간 표현 구분
  - 방문객 과거 참고값 표기
  - 날씨 미발표 문구
  - 행사 출처·신뢰도·확인 시각 표시
- 완료 조건:
  - PRD 완료 조건의 모든 표현 요구사항이 충족된다.

> 결과 화면은 혼잡 가능성과 교통 지연 예측을 구분하고, 방문객 과거 참고값·날씨 상태·행사 출처·신뢰도·확인 시각을 사용자 문구로 표시한다.

### T-096 저작권·이용약관 검수

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-068, T-087
- 작업:
  - NAVER 지도 표시·로고·데이터 이용 조건 확인
  - 관광공사와 기상청 출처표시 조건 반영
  - 행사 이미지 사용 여부와 권리 확인
  - 외부 공식 URL 연결 방식 확인
- 완료 조건:
  - 앱과 스토어 설명에 필요한 출처·고지가 반영되어 있다.

> 앱 정보 화면과 [데이터 출처·이용 고지](../source-attribution.md)에 구현 기준과 보류 항목을 반영했다. 실제 제공자 약관 및 스토어 설명 최종 확인은 배포 전에 진행한다.

### T-100 보안 기본 점검

- 상태: `[x]`
- 우선순위: `P0`
- 의존성: T-020, T-021, T-041
- 작업:
  - 임의 URL 프록시 불가 확인
  - 비정상 대용량 요청 거부
  - OpenAPI 문서 공개 범위 결정
  - CORS와 허용 메서드 최소화
  - 내부 예외 정보 노출 검사
- 완료 조건:
  - 알려진 입력 경계 우회가 차단되고 서버 정보가 과도하게 노출되지 않는다.

> 임의 URL 프록시·허용되지 않은 메서드·대용량 본문·내부 예외 메시지 노출을 자동 테스트하고, 공개 OpenAPI에는 `/health`와 분석 POST만 노출한다. 앱은 네이티브 클라이언트 전용이므로 wildcard CORS를 사용하지 않는다.

### T-102 릴리스 후보 회귀 테스트

- 상태: `[-]`
- 우선순위: `P0`
- 의존성: T-091~T-100
- 작업:
  - Android·iOS 릴리스 후보로 전체 시나리오 재실행
  - Cloud Run 후보 Revision 연동
  - 빈 결과·부분 장애·느린 네트워크 재확인
- 완료 조건:
  - 차단 등급 결함이 없고 MVP 완료 조건이 모두 체크된다.

> iOS Simulator용 XCUITest에서 `서울역`·`부산역` 후보 선택과 선택된 주소의 전체 분석을 통과했다. Android, 실기기, Cloud Run 후보 Revision 회귀는 남아 있다.

## 3. MVP 완료 체크리스트

- [-] Android와 iOS에서 동일한 핵심 흐름이 동작한다. iOS Simulator는 통과했고 Android·실기기는 대기 중이다.
- [x] 여행일이 행사 기간 밖인 행사는 제외된다.
- [x] 한 응답에 같은 행사가 중복 표시되지 않는다.
- [x] 좌표 없는 행사를 거리 기반 확정 결과로 표시하지 않는다.
- [x] 모든 확정 행사에 출처 URL이 표시된다.
- [x] 방문객 데이터가 미래 예측값으로 표현되지 않는다.
- [x] 날씨 미발표와 조회 실패가 구분된다.
- [x] 외부 API 일부 실패 시 가능한 결과가 부분 반환된다.
- [-] 서버용 API 키가 앱, 응답, 로그 및 이미지에 노출되지 않는다.
- [x] 사용자 입력과 분석 결과가 영구 저장되지 않는다.
- [x] 요청 URL과 로그에 출발지·여행지·좌표·경로가 기록되지 않는다.
- [ ] Cloud Run 최소 인스턴스 0과 최대 인스턴스 상한이 적용된다.
- [x] 요청당 목적지 행사 검색·행사 개수 집계·동시 외부 호출 상한이 적용된다. 행사 상세 조회는 후속 단계로 보류한다.
- [ ] Android 내부 테스트와 iOS TestFlight 검증이 완료된다.
- [-] 개인정보처리방침과 데이터 출처 안내가 공개된다.
